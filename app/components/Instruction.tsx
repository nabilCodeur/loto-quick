import { Text } from 'react-native';

const Instruction = () => {
    return <Text className='text-white text-xl px-2'>{"Sélectionne le dé et/ou vos numéros fétiches".toLocaleUpperCase()}</Text>
};

export default Instruction